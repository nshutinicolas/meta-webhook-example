services:
  - type: web
    name: meta-webhook-example
    runtime: node
    buildCommand: npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_VERSION
        value: 22
      - key: FACEBOOK_ACCESS_TOKEN
        sync: false
      - key: VERIFY_TOKEN
        sync: false
